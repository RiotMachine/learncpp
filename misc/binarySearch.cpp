// how does one make a list in cpp?
// how do you ensure that the desired object is even in the list?
// how do you get the object once found back to main()?
int binarySearch(list, std::string_view desiredObject)
{
    middleObject = 
    if desiredObject < middleObject
    {
        binarySearch(lowerHalflist);
    }
    else if desiredObject > middleObject
    {
        binarySearch(higherHalflist);
    }
    else
    {
        return 
    } 
}

int main()
{
    std::cout << "Which contact do you want?";
    std::string name;
    std::cin >> name;
    binarySearch(list, name)

    return 0;
}